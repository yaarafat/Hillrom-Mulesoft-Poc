http:
  port: '8081'
  
mule:
  env : 'test'
  
api:
  version: 'v1'

json:
  logger.application.version : '2.0.1'
  logger.application.name : 'covid-vaccine-service-api'

  
covid:
  port: '443'
  host: 'cdn-api.co-vin.in'
  basePath: '/api/v2/appointment/sessions/public'